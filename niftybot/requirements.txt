# Core Django & project
Django==5.0.2

# Celery + Redis + Beat (task scheduling)
celery==5.3.6
redis==5.0.1
django-celery-beat==2.6.0
django-celery-results==2.5.1          # recommended if you want to store task results in DB

# Zerodha Kite Connect
kiteconnect==4.2.0

# Data processing & utilities
pandas==2.1.4
numpy==1.24.3                         # compatible with pandas 2.1.x
python-dateutil==2.8.2
pytz==2023.3
holidays==0.36

# Authentication & OTP
pyotp==2.9.0

# HTTP & misc
requests==2.31.0
psutil==5.9.8                         # for process management in views

# Database (PostgreSQL support – optional but recommended over sqlite for production)
psycopg2-binary==2.9.9
-----------------------------------------------------------------------------------
#pip install -r requirements.txt
#pip list | findstr "Django celery redis kiteconnect pandas numpy requests pyotp pytz holidays psutil psycopg2"

#python -m pip install celery==5.3.4 django-celery-beat==2.5.0 django django-celery-results redis pandas numpy requests pyotp holidays psutil kiteconnect pytz setuptools==68.2.2


-------------------------------------------------------------------------------------

# Database (optional – PostgreSQL support)
psycopg2-binary==2.9.9
#python -m pip install celery==5.3.4
#python -m pip install django-celery-beat==2.5.0
#pip install pytz kiteconnect celery redis django-celery-beat

#python -m pip install --upgrade setuptools
#python -m pip install setuptools==68.2.2
#python -m pip install -r requirements.txt
#python -m pip install pandas
#python -m pip install holidays
#python -m pip install django-celery-results
#pip install django django-celery-beat redis pandas numpy requests pyotp holidays
#pip install psutil
#pip install celery
#pip install django-redis redis


